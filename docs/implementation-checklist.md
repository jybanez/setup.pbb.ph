# PBB Kit Setup Implementation Checklist

This checklist tracks implementation against the 12-step non-technical installer flow.

## Status Legend

- `[x]` implemented and smoke-tested
- `[~]` partially implemented
- `[ ]` not implemented

## Flow Checklist

| Step | Area | Status | Current Artifact | Next Implementation Gate |
|---:|---|---:|---|---|
| 1 | Platform Check | [~] | `detect` runner action probes PHP, paths, Apache, MySQL/MariaDB, ffmpeg/ffprobe, services, and optional port checks | Add richer visual readiness screen |
| 2 | Hub Pairing | [~] | `hub-resolve` action, Hub config schema, redacted reports | Add visual confirmation screen and persisted pairing state |
| 3 | Select Apps For This Machine | [~] | Desktop app local/remote/off controls, `machine.selected_apps`, app `install_scope` schema fields | Add dependency-aware remote requirement prompts |
| 4 | Choose Base Path | [~] | Desktop config builder derives local app install/public paths from one base path | Add folder creation preview |
| 5 | Prepare Trusted App Packages | [x] | `prepare-packages` action, trusted package manifest, checksum validation, OpenSSL detached signature verification, ZIP staging, target backup, disposable archive deployment | Wire production app release archives into the manifest |
| 6 | Network & Local DNS | [~] | `dns-plan`, guarded `dns-apply`, and `dns-verify` actions for Technitium records and resolver checks | Test live Technitium apply on a node machine |
| 7 | SSL & Web Server | [~] | `ssl-plan` and guarded `ssl-apply` validate cert/key match, SAN coverage, PEM extraction, Apache vhost include writes, and Apache config test after apply | Add guarded service reload/restart |
| 8 | Remote Dependency Check | [~] | `remote-check` action validates remote app DNS, optional runtime credentials, and HTTP health endpoints | Test against protected remote app health endpoints |
| 9 | Admin Account | [~] | Desktop runtime password field, shared first-admin contract, `password_env` resolution, and config injection | Add password strength validation in visual setup |
| 10 | Installation Plan | [~] | `plan` action writes consolidated platform, package, DNS, SSL, remote dependency, and app summaries into `kit-report.json` | Add visual review/confirm screen |
| 11 | Stage-By-Stage Install | [~] | Desktop shell scaffold, focused stage panels, per-stage validation, confirmation gates, checkpoint grid, per-app retry, `stage-report`, `preflight`, `install`, `populate`, and `smoke-check` actions | Test smoke checks on a fully installed node |
| 12 | Finish | [~] | `finish-report` action, desktop finish summary, Apache config-test after vhost apply, and unpacked desktop packaging target | Add guarded service reload/status controls and production installer signing |

## Security Checklist

- [x] Hub token can be supplied through environment variables.
- [x] Hub reports redact token material.
- [x] Runtime outputs stay under ignored `storage/`.
- [x] First-admin password can be supplied through an environment variable for unattended harness runs.
- [x] Cross-app secrets can be generated by Kit Setup and written to ignored runtime storage.
- [x] Secret reports expose only names, lengths, and short hashes.
- [x] Technitium API tokens can be referenced through environment variables and are not written to DNS reports.
- [x] Selected apps must have a trusted package manifest entry before package preparation passes.
- [~] App packages must come only from bundled or official trusted sources.
- [x] Package signatures can be verified before extraction with detached OpenSSL SHA-256 signatures.
- [x] PEM private keys must never be written to reports or logs.
- [x] Technitium API tokens must be accepted through environment variables and redacted from reports.
- [x] Visual installer must not allow arbitrary app installer upload.

## Current Engineering Slice

- [x] Add Hub resolution backend.
- [x] Add platform detection backend.
- [x] Add Apache/MySQL/ffmpeg/service manager platform probes.
- [x] Add schema vocabulary for platform, machine, layout, packages, domains, DNS, SSL, and app install scope.
- [x] Run all local app preflights through the Kit harness.
- [x] Add run-scoped cross-app secret generation.
- [x] Run MapServer, Maestro, and Realtime population dry-runs through the Kit harness.
- [x] Run Hotline population dry-run through the Kit harness.
- [x] Complete all-app population dry-run gate.
- [x] Add trusted package manifest format.
- [x] Add package preparation action.
- [x] Run package preparation dry-run across all selected local apps.
- [x] Add ZIP archive extraction into staging.
- [x] Add safe deployment into allowed target roots with backup.
- [x] Add cryptographic signature verification for production release archives.
- [x] Add DNS plan action.
- [x] Add guarded Technitium DNS apply action.
- [x] Add live DNS verification after apply.
- [x] Add SSL/vhost action.
- [x] Add PEM bundle split/write support.
- [x] Add guarded Apache vhost apply.
- [~] Add Apache/Nginx reload checks.
- [x] Add remote dependency DNS/HTTP health checks.
- [x] Add remote dependency credential validation.
- [x] Add consolidated installation plan summary.
- [x] Add non-destructive 12-step visual stage report.
- [x] Record desktop app, secret handling, Technitium, Apache include, and package-source decisions.
- [x] Add external-machine dry-run guide.
- [x] Scaffold desktop installer shell.
- [x] Add initial desktop setup inputs and runtime config builder.
- [x] Split setup inputs into focused per-stage panels.
- [x] Add per-stage validation and inline recovery guidance.
- [x] Add mutation confirmation gates.
- [x] Add same-run action checkpoints and desktop checkpoint grid.
- [x] Add per-app retry/resume for partial install failures.
- [x] Add finish report and desktop finish summary.
